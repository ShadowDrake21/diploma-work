import { OverviewHasDataPipe } from './overview-has-data.pipe';

describe('OverviewHasDataPipe', () => {
  it('create an instance', () => {
    const pipe = new OverviewHasDataPipe();
    expect(pipe).toBeTruthy();
  });
});
