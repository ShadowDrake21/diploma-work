import { LocalProjectTypePipe } from './local-project-type.pipe';

describe('LocalProjectTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalProjectTypePipe();
    expect(pipe).toBeTruthy();
  });
});
