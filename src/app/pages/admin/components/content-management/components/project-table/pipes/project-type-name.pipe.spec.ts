import { ProjectTypeNamePipe } from './project-type-name.pipe';

describe('ProjectTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
