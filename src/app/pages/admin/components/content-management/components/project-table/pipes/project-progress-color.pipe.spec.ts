import { ProjectProgressColorPipe } from './project-progress-color.pipe';

describe('ProjectProgressColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectProgressColorPipe();
    expect(pipe).toBeTruthy();
  });
});
